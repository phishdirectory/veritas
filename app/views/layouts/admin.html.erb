<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <link rel="mask-icon" href="/safari-pinned-tab.svg" color="#2F2F2F" />
    <meta name="msapplication-TileColor" content="#2F2F2F" />
    <meta name="apple-mobile-web-app-title" content="Veritas Admin" />
    <meta name="application-name" content="Veritas Admin" />
    <meta name="description" content="Admin panel for Veritas authentication service." />
    <meta name="viewport" content="width=device-width,initial-scale=1">

    <title><%= content_for(:title) || "Admin" %> - Veritas</title>

    <%= yield :head %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <style>
      body {
        background-color: #f7fafc;
      }

      .admin-sidebar {
        background-color: #2d3748;
        color: white;
      }

      .admin-nav-link {
        color: #cbd5e0;
        transition: all 0.2s;
      }

      .admin-nav-link:hover,
      .admin-nav-link.active {
        color: white;
        background-color: #4a5568;
      }
    </style>
  </head>

  <body class="bg-gray-50">
    <% if Rails.env.staging? %>
      <div class="bg-red-600 text-white text-center py-2">
        <strong>STAGING ENVIRONMENT</strong> - Do not use for production
      </div>
    <% end %>

    <div class="min-h-screen flex">
      <!-- Sidebar -->
      <div class="admin-sidebar w-64 flex-shrink-0">
        <div class="flex flex-col h-full">
          <!-- Logo/Header -->
          <div class="flex items-center justify-center h-16 px-4 border-b border-gray-700">
            <h1 class="text-xl font-bold">Veritas Admin</h1>
          </div>

          <!-- Navigation -->
          <nav class="flex-1 px-4 py-6 space-y-2">
            <%= link_to admin_root_path,
                class: "admin-nav-link flex items-center px-4 py-2 text-sm rounded-lg #{controller_name == 'dashboard' ? 'active' : ''}" do %>
              <span>Dashboard</span>
            <% end %>

            <%= link_to admin_users_path,
                class: "admin-nav-link flex items-center px-4 py-2 text-sm rounded-lg #{controller_name == 'users' ? 'active' : ''}" do %>
              <span>Users</span>
            <% end %>

            <%= link_to admin_services_path,
                class: "admin-nav-link flex items-center px-4 py-2 text-sm rounded-lg #{controller_name == 'services' ? 'active' : ''}" do %>
              <span>Services</span>
            <% end %>
          </nav>

          <!-- User Info -->
          <div class="border-t border-gray-700 p-4">
            <div class="flex items-center">
              <div class="ml-3">
                <p class="text-sm font-medium text-white"><%= current_user.full_name %></p>
                <p class="text-xs text-gray-400"><%= current_user.access_level.titleize %></p>
              </div>
            </div>
            <div class="mt-3">
              <% if impersonating? %>
                <%= button_to "Stop Impersonating", stop_impersonating_admin_users_path,
                    method: :delete,
                    class: "admin-nav-link flex items-center px-4 py-2 text-sm rounded-lg w-full text-left",
                    form: { class: "w-full" } %>
              <% else %>
                <%= button_to "Sign Out", logout_path,
                        method: :delete,
                        form_class: "inline-block",
                        class: "text-white hover:text-gray-200 px-3 py-1.5 rounded-md bg-gray-700 text-sm transition-colors border-0 cursor-pointer" %>
              <% end %>
            </div>
          </div>
        </div>
      </div>

      <!-- Main Content -->
      <div class="flex-1 overflow-auto">
        <%= render 'shared/flash' if respond_to?(:render) && lookup_context.exists?('shared/flash') %>

        <%= yield %>
      </div>
    </div>

    <!-- Flash Messages -->
    <% flash.each do |type, message| %>
      <div class="fixed top-4 right-4 z-50 max-w-sm">
        <div class="bg-white border border-gray-200 rounded-lg shadow-lg p-4">
          <div class="flex items-center">
            <div class="flex-1">
              <p class="text-sm text-gray-900"><%= message %></p>
            </div>
            <button onclick="this.parentElement.parentElement.parentElement.remove()"
                    class="ml-4 text-gray-400 hover:text-gray-600">
              <span class="sr-only">Close</span>
              ×
            </button>
          </div>
        </div>
      </div>
    <% end %>

    <script>
      // Auto-hide flash messages after 5 seconds
      document.addEventListener('DOMContentLoaded', () => {
        const flashMessages = document.querySelectorAll('.fixed.top-4.right-4');
        flashMessages.forEach(message => {
          setTimeout(() => {
            if (message.parentElement) {
              message.remove();
            }
          }, 5000);
        });
      });
    </script>
  </body>
</html>
